services:
  api:
    build: ./server
    container_name: illegal-construction-api
    restart: always
    ports:
      - "5001:5001"
    env_file:
      - ./server/.env
    environment:
      - NODE_ENV=production