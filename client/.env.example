# Local dev: direct API (CORS allows http://localhost:5173).
# Or use /api/v1 if you prefer the Vite proxy in vite.config.ts.
VITE_API_URL=http://localhost:5001/api/v1

# Production build (see .env.production): point at the Render API service URL.
